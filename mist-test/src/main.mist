mod app;
mod task;
mod plugin;
mod logger;
mod event;

void main() {
    var mut app_instance = new app::Application("mist-app");

    // greeting::register(&mut app_instance);
    // statistics::register(&mut app_instance);

    app_instance.boot();
    app_instance.run();
}