function add(a: int, b: int): int {
    let sum = 0;

        if (i == 5) {
            continue;
        }

        sum = sum + i;

    if (sum > 20) {
        return sum;
    } else {
        return 0;
    }
}
