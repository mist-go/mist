mod my_class;
use <my_class::MyClass>;
use <my_class::MyEnum>;

struct Test<'a> {}

void main() {
    var my_class = new MyClass("World");
    my_class.greet();

    match (MyEnum::Tuple(10, String::new())) {
        MyEnum::Tuple(i, _) => {
            println!("{i}");
        }
        MyEnum::Named => {

        }
        _ => {}
    }
}
